apiVersion: v1
kind: Service
metadata:
  name: hello-api
  namespace: hello-api
  labels:
    app: hello-api
spec:
  selector:
    app: hello-api
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
